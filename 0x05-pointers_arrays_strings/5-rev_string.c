#include "main.h"

/**
 * rev_string -> changinh value of number through pointers
 * @s: variable
 */

void rev_string(char *s)
{
	int a, i;

	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}
	i--;
	for (a = 0; j <= i; i--)
	{
		_putchar(s[i]);
	}

	_putchar('\n');
}
