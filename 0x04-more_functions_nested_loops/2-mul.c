#include "main.h"

/**
 * mul -> checks letters for lowercase
 * @a: the first number to multibly
 * @b: the second number to multibly
 * Return: 1 Success
 */

int mul(int a, int b)
{
	return (a * b);
}
